# Caddyfile — configuration reverse proxy prod.
# Monté dans le conteneur caddy (compose prod). Aucune valeur réelle ici.
# Caddy gère seul l'obtention/renouvellement des certificats Let's Encrypt.

{
  # E-mail ACME injecté via la variable d'environnement ACME_EMAIL.
  email {$ACME_EMAIL}
}

leonheu.fr, www.leonheu.fr {
  encode gzip zstd
  reverse_proxy web:3000

  header {
    Strict-Transport-Security "max-age=15552000; includeSubDomains"
    X-Frame-Options "DENY"
    X-Content-Type-Options "nosniff"
    Referrer-Policy "strict-origin-when-cross-origin"
    Permissions-Policy "camera=(), microphone=(), geolocation=()"
  }

  # Cache statique long pour /_next/static et /uploads
  @static path /_next/static/* /uploads/*
  header @static Cache-Control "public, max-age=31536000, immutable"

  log {
    output file /var/log/caddy/access.log {
      roll_size 50mb
      roll_keep 14
    }
  }
}

staging.leonheu.fr {
  encode gzip zstd
  reverse_proxy web-staging:3000

  header {
    Strict-Transport-Security "max-age=15552000; includeSubDomains"
    X-Frame-Options "DENY"
    X-Content-Type-Options "nosniff"
    Referrer-Policy "strict-origin-when-cross-origin"
    X-Robots-Tag "noindex, nofollow"
  }
}

# Optionnel Sprint 7+ : Storybook avec basic auth
# storybook.leonheu.fr {
#   basicauth {
#     leon $2a$14$... # bcrypt: caddy hash-password
#   }
#   reverse_proxy storybook:6006
# }
